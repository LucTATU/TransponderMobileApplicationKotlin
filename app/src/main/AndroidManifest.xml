<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.Manifest.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="fr.tatu.kartingapp.ble.BLEDeviceActivity" />
        <activity android:name="fr.tatu.kartingapp.ble.BLEScanActivity" />
        <activity android:name="fr.tatu.kartingapp.firebase.karting.DataKartActivity" />
        <activity android:name="fr.tatu.kartingapp.firebase.karting.SessionDuActivity" />
        <activity android:name="fr.tatu.kartingapp.firebase.karting.SessionSelectionActivity" />
        <activity android:name="fr.tatu.kartingapp.firebase.karting.ChoiceDaySessionActivity" />
        <activity android:name="fr.tatu.kartingapp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="fr.tatu.kartingapp.firebase.LoginActivity" />
        <activity android:name="fr.tatu.kartingapp.firebase.RegisterActivity" />
    </application>

</manifest>