<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.Manifest.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="fr.tatu.kartingapp.BLEDeviceActivity" />
        <activity android:name="fr.tatu.kartingapp.BLEScanActivity" />
        <activity android:name="fr.tatu.kartingapp.InfosKartActivity" />
        <activity android:name="fr.tatu.kartingapp.SessionDuActivity" />
        <activity android:name="fr.tatu.kartingapp.SessionSelectionActivity" />
        <activity android:name="fr.tatu.kartingapp.ChoiceDaySessionActivity" />
        <activity android:name="fr.tatu.kartingapp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="fr.tatu.kartingapp.LoginActivity" />
        <activity android:name="fr.tatu.kartingapp.RegisterActivity" />
    </application>

</manifest>